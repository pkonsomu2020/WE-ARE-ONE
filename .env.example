# Frontend Environment Variables

# API Base URL - Update this after backend deployment
# Note: The /api suffix will be added automatically if not present
VITE_API_BASE_URL=https://we-are-one-api.onrender.com

# Or use custom domain after DNS setup
# VITE_API_BASE_URL=https://api.weareone.co.ke